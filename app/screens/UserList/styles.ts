import {
  StyleSheet,
  ViewStyle,
} from "react-native";

type Styles = {
  activityIndicator: ViewStyle,
};

export default StyleSheet.create<Styles>({
  activityIndicator: {
    margin: 24,
  }
})